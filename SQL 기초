SQL (short for Structured Query Language) is a programming language designed to manage and retrieve data stored in databases.

---------02. SELECT * form shows; //this will how all columns--

--this just select a few of the columns--
Select id,name, genre
from shows;


-------------- 04. WHERE( filter information based on a condition)


--Select * 
--from shows
--where id = 23;

Select * 
from shows
where year > 2020;


----------- 05. LIKE (search for a pattern in a column.)

--1. A%  values that begin with letter 'A'.
--2. %z  values that end with 'z'.

SELECT * 
FROM shows 
WHERE genre LIKE %com%;



---------- 06.ORDER BY( sorts data in ascending or descending order. By default data in ascending order.)


--SELECT name, genre, stream, year
--FROM shows
--ORDER BY year;


--SELECT name, genre, stream, year
--FROM shows
--ORDER BY year DESC;

Select name, genre, stream,year, tomatometer
From shows
Order by tomatometer DESC;

----------------------------------------------------------

-- COUNT(): returns the number of rows
-- MAX(): returns the largest value in a column.
-- MIN(): returns the smallest value in a column.
-- SUM(): returns the total sum in a column.
-- AVG(): returns the average value in a column.

--GROUP BY: groups rows of data with the same values into buckets.






